<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic (Supabase) is HTTPS. Cleartext is permitted only for the
  loopback addresses used when testing against a PostgREST instance running on
  the developer machine or the Android emulator host (10.0.2.2).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
