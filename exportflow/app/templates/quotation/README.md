# Quotation templates

Quotation PDFs are composed programmatically by `app/reports/quotation_pdf.py`
(ReportLab) so that the layout stays consistent across the three interface
languages. Wording is kept in the `_TEXT` table of that module.

Place custom letterhead images here and point the company profile logo at them.
