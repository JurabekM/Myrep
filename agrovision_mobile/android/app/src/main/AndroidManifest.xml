<?xml version="1.0" encoding="utf-8"?>
<!--
    AgroVision Mobile — hibrid rejim: ilova hech qanday server bilan doimiy
    sinxronizatsiya QILMAYDI, foydalanuvchi ma'lumotlari (fermer/xo'jalik/
    dala/hosildorlik/sug'orish/moliya/NDVI/bozor narxi) faqat qurilma ichida
    saqlanadi. INTERNET ruxsati faqat OB-HAVO modulida Open-Meteo'ning ochiq,
    kalitsiz API'sidan HAQIQIY tarixiy/joriy ma'lumot olish uchun ishlatiladi
    (internet bo'lmasa, ilova mahalliy keshdagi so'nggi ma'lumot bilan
    to'liq ishlayveradi — network.WeatherApi ichidagi try/catch orqali).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".AgroVisionApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroVision"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AgroVision">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
